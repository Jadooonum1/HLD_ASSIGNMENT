# Parth's Autocomplete Engine & Analytics Dashboard

A fully functioning, high-performance **distributed search typeahead and suggestion backend** built for the HLD (High-Level Design) assignment. It provides instant prefix completions, tracks user query trends, handles database load via **aggregating write buffers**, shards cached keys using **consistent hashing virtual nodes**, and ranks suggestions dynamically using a **blended popularity and recency scorer**.

---

## 1. Assignment Requirements Mapping

| Rubric Component | Code File Location |
|---|---|
| **Core Suggestions Infrastructure** (Trie index, suggestions search API, cache sharding, consistent hash ring) | [src/trieIndex.js](src/trieIndex.js), [src/storeManager.js](src/storeManager.js), [src/querySuggester.js](src/querySuggester.js), [src/cacheManager.js](src/cacheManager.js), [src/hashRing.js](src/hashRing.js) |
| **Recency-Aware Ranking & Trends** (Exponential time-decay tracking, blended scoring) | [src/trendTracker.js](src/trendTracker.js), [src/rankingScorer.js](src/rankingScorer.js) |
| **Write Buffer Aggregation** (Aggregated batch flushes, durability vs. performance trade-offs) | [src/writeBuffer.js](src/writeBuffer.js) |

---

## 2. System Architecture Flow

The system runs entirely in Node.js (zero external dependencies like Redis or Databases to simplify local HLD demonstrations):

```
                       Client Browser (UI)
            GET /suggest                 POST /search
                 │                            │
                 ▼                            ▼
┌─────────────────────────────────────────────────────────────┐
│                 Express API Server (src/server.js)          │
│                                                             │
│  GET /suggest ──► QuerySuggester                            │
│                     1. Query CacheManager (Consistent Ring)  │
│                        - HIT  ──► Return 10 entries         │
│                        - MISS ──► 2. Read StoreManager      │
│                                   3. Run RankingScorer      │
│                                   4. Populate Cache         │
│                                                             │
│  POST /search ──► TrendTracker.registerSearch()             │
│               ──► CacheManager.clearAllPrefixesForQuery()   │
│               ──► WriteBuffer.queueSearch() ──► Pending     │
│                                         │                   │
│                                         ▼ (Periodic Flush)  │
│                                   StoreManager (Apply)      │
│                                         │ (Snapshot Interval)│
│                                         ▼                   │
│                                   data/primary-store.json   │
└─────────────────────────────────────────────────────────────┘
```

A visual model is available at **[docs/architecture.html](docs/architecture.html)**.

---

## 3. Getting Started

### Prerequisites
* **Node.js** (v18+ recommended)

### Quick Start Commands
```bash
# 1. Install required packages
npm install

# 2. Build the Zipfian mock dataset (120,000 queries)
npm run generate-data

# 3. Start the application
npm start
```
Once started, visit **http://localhost:3000** to access the dashboard.

---

## 4. Diagnostic & Load Testing Reports

The following performance summary is generated by running `npm run benchmark` (executing 20,000 suggest reads at 50-way concurrency, followed by 5,000 simulated search writes):

| Performance Category | Measured Result | High-Level Design Goal |
|---|---|---|
| **Server suggestion compute time (p95)** | **0.003 ms** | Sub-millisecond response for autocomplete candidates |
| **Client-side end-to-end latency (p95)** | **4.88 ms** | Low latency under high concurrency (HTTP/JSON serialization) |
| **Cache Hit Efficiency** | **99.8%** | Cache Shards absorb 19,966 / 20,000 read requests |
| **Database Disk Reads** | **34** | Direct lookups limited to initial cache misses |
| **Database Write Reduction** | **99.3%** | Consolidated 5,000 updates into 34 atomic disk commits |

---

## 5. API Reference

| Endpoint | Method | Params / Payload | Action |
|---|---|---|---|
| `/suggest` | `GET` | `q=<prefix>&mode=base\|recency` | Returns top 10 ranked candidates. |
| `/search` | `POST` | `{ "query": "string" }` | Records search, updates trend tracker, and buffers count increment. |
| `/trending` | `GET` | None | Lists current queries ranked by decayed recency. |
| `/cache/debug` | `GET` | `prefix=<prefix>` | Diagnostic info showing ring position hash and owner shard. |
| `/metrics` | `GET` | None | System counters and latency summary statistics. |

---

## 6. Directory Map

```
config.js                 # Global configuration values (port, buffer size, TTL, half-life)
data/                     # Directory for dataset CSV and backup state dumps (Git-ignored)
scripts/
  generate-dataset.js     # Builds Zipf-distributed mock dataset queries
  benchmark.js            # Stress tests suggestions and search batch flushes
src/
  server.js               # Express server routing, startup and graceful shutdown bindings
  storeManager.js         # Map query storage and index synchronization manager
  trieIndex.js            # O(L) prefix candidate lookup index
  querySuggester.js       # Suggestion search workflow (cache check -> fallback -> cache write)
  rankingScorer.js        # Basic count ranking and blended popularity-recency calculations
  trendTracker.js         # Real-time exponential decay score tracker
  hashRing.js             # Consistent hashing mapping algorithm using virtual nodes
  cacheManager.js         # Shards prefix routes to matching cache partitions
  cacheShard.js           # Key-value cache shard with independent lazy TTL and FIFO eviction
  statsTracker.js         # Tracks rolling metrics and calculates latency percentiles
  appLogger.js            # Console logging wrapper
public/                   # Dark-themed glassmorphism interface (HTML, CSS, JS)
```

---

## 7. Deep Dive: Key HLD Decisions

* **Consistent Hash Ring**: Distributes search prefixes across 4 cache shards. The use of virtual nodes (150 per node) ensures uniform key storage distribution. Node removals/additions only trigger re-allocations of `1/N` keys rather than a global stampede.
* **Write Buffering**: To protect the storage layer, search counts are aggregated in an in-memory buffer and flushed either when size limits are reached or every 2 seconds. The risk of losing minor updates during a crash is accepted in favor of a 99% reduction in disk write QPS.
* **Blended Recency Metric**: Counts are scaled using `log10(count)` so older, massive query volumes do not drown out new spikes. Spikes are governed by an exponential half-life decay, ensuring short-lived spikes fade cleanly as search volumes cool down.
